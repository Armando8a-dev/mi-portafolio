---
// src/components/About.astro
import SkillIcon from "./SkillIcon.astro";
---

<section id="about" class="page-section">
  <div class="container">
    <div class="heading-container">
      <h2 class="heading-main">Sobre Mí</h2>
      <p class="heading-sub">
        Aquí encontrarás más información sobre mí, lo que hago y mis
        habilidades.
      </p>
    </div>

    <div class="about-content">
      <!-- Columna 1: Conóceme -->
      <div class="about-column">
        <h3 class="content-title">¡Conóceme!</h3>
        <p>
          Soy <strong>Desarrollador Full Stack</strong> con especial enfoque en <strong>Frontend</strong>. Me
          motiva construir interfaces accesibles, atractivas y de alto
          rendimiento que generen valor en cada proyecto. Actualmente,
          complemento mi trayectoria con formación como <strong>Analista de datos</strong>, lo que
          me permite proponer soluciones basadas en información objetiva para
          optimizar productos digitales y procesos.
        </p>
        <a href="#contact" class="btn">Contáctame</a>
      </div>

      <!-- Columna 2: Mis Habilidades -->
      <div class="about-column">
        <h3 class="content-title">Mis Habilidades</h3>
        <div class="skills-grid">
          <SkillIcon name="Angular" icon="simple-icons:angular" />
          <SkillIcon name="React" icon="simple-icons:react" />
          <SkillIcon name="Astro" icon="simple-icons:astro" />
          <SkillIcon name="JavaScript" icon="simple-icons:javascript" />
          <SkillIcon name="TypeScript" icon="simple-icons:typescript" />
          <SkillIcon name="HTML" icon="simple-icons:html5" />
          <SkillIcon name="CSS" icon="simple-icons:css3" />
          <SkillIcon name="Node.js" icon="simple-icons:nodedotjs" />
          <SkillIcon name="Java" icon="simple-icons:openjdk" />
          <SkillIcon name="Spring" icon="simple-icons:spring" />
          <SkillIcon name="Python" icon="simple-icons:python" />
          <SkillIcon name="Pandas" icon="simple-icons:pandas" />
          <SkillIcon name="Tableau" icon="simple-icons:tableau" />
          <SkillIcon name="PostgreSQL" icon="simple-icons:postgresql" />
          <SkillIcon name="OpenShift" icon="simple-icons:redhatopenshift" />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .page-section {
    padding: 5rem 0;
  }
  .container {
    max-width: 1100px;
    width: 90%;
    margin: 0 auto;
  }

  /* --- Títulos --- */
  .heading-container {
    text-align: center;
    margin-bottom: 4rem;
  }
  .heading-main {
    font-size: var(--font-size-xl);
    margin-bottom: 1rem;
  }
  .heading-sub {
    font-size: var(--font-size-md);
    color: var(--color-texto-secundario);
    max-width: 600px;
    margin: 0 auto;
  }

  /* --- Contenido de 2 Columnas --- */
  .about-content {
    display: grid;
    grid-template-columns: 1fr 1fr; /* Dos columnas en escritorio */
    gap: 4rem;
    align-items: flex-start; /* Alinea las columnas por la parte superior */
  }
  .content-title {
    font-size: var(--font-size-lg);
    margin-bottom: 1.5rem;
  }
  .about-column p {
    font-size: var(--font-size-md);
    line-height: 1.7;
    color: var(--color-texto-secundario);
  }

  /* --- Cuadrícula de Habilidades --- */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
    gap: 1rem;
  }

  /* --- Botón --- */
  .btn {
    display: inline-block;
    margin-top: 1.5rem;
    padding: 0.75rem 1.5rem;
    background: var(--color-texto-principal);
    color: #fff;
    text-decoration: none;
    border-radius: 5px;
    font-weight: 700;
    transition: background-color 0.3s;
  }
  .btn:hover {
    opacity: 0.9;
  }

  /* --- REGLAS PARA RESPONSIVO (MÓVIL) --- */
  @media (max-width: 900px) {
    .about-content {
      grid-template-columns: 1fr; /* UNA SOLA COLUMNA EN MÓVIL */
    }
  }

  /* --- Media Query para móviles --- */
  @media (max-width: 768px) {
    .heading-main { /* <-- Reemplaza si tu selector es diferente (ej. h1) */
      font-size: 2.5rem; /* <-- Ajusta este valor hasta que se vea bien */
    }
  }
</style>
