---
import { Icon } from "astro-icon/components";

// src/components/Hero.astro
---

<section class="hero">
  <div class="hero-content">
    <h1 class="title">Hola, soy Armando Ochoa</h1>
    <p class="description">
      Un desarrollador web orientado a resultados que crea y administra sitios
      y aplicaciones web que conducen al éxito del producto general.
    </p>
  </div>
  <div class="scroll-indicator">
    <div class="mouse"></div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh; /* Usa min-height para más flexibilidad */
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center; /* Centra el contenido horizontalmente */
    position: relative;
    padding: 2rem;
    text-align: center; /* Centra el texto por defecto */
  }

  .hero-content {
    max-width: 750px; /* Limita el ancho del texto para mejor legibilidad */
  }

  .title {
    font-size: clamp(2.5rem, 8vw, 4rem); /* Título grande y responsivo */
    color: var(--color-texto-principal);
    margin: 0 0 1rem 0;
  }

  .description {
    font-size: var(--font-size-md);
    color: var(--color-texto-secundario);
    line-height: 1.7;
    margin: 0;
  }

  /* --- Animación del Mouse --- */
  .scroll-indicator {
    position: absolute;
    bottom: 2rem; /* Posiciona en la parte inferior */
    left: 50%;
    transform: translateX(-50%);
  }

  .mouse {
    width: 25px;
    height: 40px;
    border: 2px solid var(--color-texto-secundario);
    border-radius: 60px;
    position: relative;
  }

  .mouse::before {
    content: "";
    width: 5px;
    height: 5px;
    position: absolute;
    top: 7px;
    left: 50%;
    transform: translateX(-50%);
    background-color: var(--color-texto-secundario);
    border-radius: 50%;
    opacity: 1;
    animation: wheel 1.5s infinite;
  }

  @keyframes wheel {
    from {
      opacity: 1;
      top: 7px;
    }
    to {
      opacity: 0;
      top: 25px;
    }
  }

</style>
