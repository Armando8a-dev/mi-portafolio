---
// src/components/Hero.astro
---

<section class="hero">
  <div class="hero-content">
    <h1 class="title">HOLA, SOY ARMANDO OCHOA</h1>
    <p class="description">
      Apasionado por crear aplicaciones y sitios web funcionales, optimizados y
      orientados a resultados. Busco combinar mi experiencia en desarrollo con
      el análisis de datos para impulsar la toma de decisiones y el crecimiento
      de los productos digitales.
    </p>
  </div>
  <div class="scroll-indicator">
    <div class="mouse"></div>
  </div>
</section>

<style>
  .hero {
    /* SOLUCIÓN: Controla el espacio con padding, no con min-height ni flexbox */
    padding: 6rem 2rem; /* Espacio generoso arriba y abajo */
    position: relative;
    text-align: center;
  }

  .hero-content {
    max-width: 1000px;
    margin: 0 auto; /* Centra el contenedor de texto horizontalmente */
  }

  .title {
    font-size: clamp(2.2em, 8vw, 4rem);
    color: var(--color-texto-principal);
    margin: 0 0 1.5rem 0;
  }

  .description {
    font-size: 1.1rem;
    color: var(--color-texto-secundario);
    line-height: 1.7;
    margin: 0;
  }

  /* --- Animación del Mouse --- */
  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
  }

  .mouse {
    width: 25px;
    height: 40px;
    border: 2px solid var(--color-texto-secundario);
    border-radius: 60px;
    position: relative;
  }

  .mouse::before {
    content: "";
    width: 5px;
    height: 5px;
    position: absolute;
    top: 7px;
    left: 50%;
    transform: translateX(-50%);
    background-color: var(--color-texto-secundario);
    border-radius: 50%;
    opacity: 1;
    animation: wheel 1.5s infinite;
  }

  @keyframes wheel {
    from {
      opacity: 1;
      top: 7px;
    }
    to {
      opacity: 0;
      top: 25px;
    }
  }
</style>
